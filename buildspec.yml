version: 0.2

phases:
  pre_build:
    commands:
      - echo "Installing dependencies..."
      - npm install

  build:
    commands:
      - echo "Building the Node.js application..."
      - npm run build

artifacts:
  files: '**/*'